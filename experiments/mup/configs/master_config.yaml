simulation_parameters:
  network:
    base_layer_widths:
      Dense_0: 128
      Dense_1: 128
      Dense_2: 10
    param_scheme: muP
  training:
    lr: 0.001
    wd: 0.01
    task: /scratch/m/M.Rautenberg/projects/mup/datasets/cifar-10-batches-py
    test_split: 0.2
    batch_size: 100
    epochs: 1000     # Epochs refers to the number of full passes through the training data
    save_loss_frequency: 100
    loss_type: "cross_entropy"